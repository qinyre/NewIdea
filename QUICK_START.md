# 🚀 快速启动指南

## 验证修复成果

### 1️⃣ 后端测试
```bash
cd backend
python -m pytest tests/ -v
```
✅ 应该看到：29 passed

### 2️⃣ 前端构建
```bash
cd frontend
npm run build
```
✅ 应该看到：✓ built in ~1s

### 3️⃣ 启动服务

**终端 1 - 后端：**
```bash
cd backend
# 确保已安装依赖
pip install -r requirements.txt
# 设置环境变量（根据需要）
# export OPENAI_API_KEY=your-key
uvicorn app.main:app --reload
```

**终端 2 - 前端：**
```bash
cd frontend
npm install  # 首次运行
npm run dev
```

### 4️⃣ 体验新功能

访问 http://localhost:5173

#### ✨ 新功能 1：快速开始
1. 点击"创建游戏"标签
2. 看到 3 个蓝色预设按钮
3. 点击"5个 GPT-4o-mini（推荐）"
4. 点击"🎮 创建游戏"

#### ✨ 新功能 2：表单验证
1. 手动清空某个玩家的 ID
2. 看到红色边框 + 错误提示
3. 填入重复的 ID
4. 看到"玩家 ID 重复"错误
5. 修正后错误自动消失

#### ✨ 新功能 3：事件时间线
1. 游戏创建后自动跳转到"当前游戏"
2. 向下滚动看到"事件时间线"卡片
3. 实时看到游戏事件更新
4. 每个事件带时间戳和彩色标签

#### ✨ 新功能 4：查看 AI 推理
1. 在事件时间线中找到带"查看推理"按钮的事件
2. 点击展开
3. 看到黄色高亮的 💭 推理内容
4. 点击"收起"隐藏

#### ✨ 新功能 5：游戏回放
1. 等待游戏完成
2. 进入"历史记录"标签
3. 点击任意游戏
4. 查看完整事件流（可回放）

---

## 🎯 核心改进对比

### 修复前
- ❌ 只能看到"游戏结束，好人胜利"
- ❌ 不知道中间发生了什么
- ❌ 看不到 AI 的思考过程
- ❌ 需要手动配置 5 个玩家
- ❌ 表单错误提交后才发现

### 修复后
- ✅ 完整事件时间线
- ✅ 每个玩家的发言、投票、动作
- ✅ AI 的推理过程可展开查看
- ✅ 一键快速开始
- ✅ 实时表单验证

---

## 📋 技术细节

### 新增 API 端点
```
GET /api/games/{game_id}/events
```

返回完整事件流，包含：
- 游戏开始/结束
- 玩家发言
- 投票和结果
- 狼人击杀（带推理）
- 预言家查验（带推理）
- 阶段变更
- 玩家死亡

### 新增前端组件
```
frontend/src/components/EventTimeline.tsx
```

功能：
- 事件类型图标和颜色
- 时间戳显示
- 推理内容展开/收起
- 可见性标签（公开/私密）
- 自动轮询更新

### 持久化改进
```
backend/data/
  ├── games.json                    # 游戏元数据
  └── game-{id}_events.json         # 事件流（独立文件）
```

---

## 🐛 常见问题

**Q: 前端显示"加载 provider 列表失败"**
- 确认后端已启动（http://localhost:8000/health）
- 检查后端日志是否有错误

**Q: 创建游戏时提示 API key 未设置**
- 在 `backend/.env` 中设置对应的 API key
- 或使用"自定义端点"选项

**Q: 事件时间线显示"加载事件失败"**
- 游戏刚创建时事件为空，等待几秒
- 游戏完成后事件会持久化到文件

**Q: 看不到 AI 推理**
- 只有部分事件有推理（击杀、查验）
- 点击"查看推理"按钮展开

---

## 🎉 验收标准

运行以下检查，确认修复成功：

- [ ] 后端测试全部通过（29 passed）
- [ ] 前端构建成功（无错误）
- [ ] 快速开始按钮可以一键创建游戏
- [ ] 表单验证实时生效
- [ ] 事件时间线显示游戏过程
- [ ] 可以查看 AI 推理内容
- [ ] 历史游戏可以回放
- [ ] 私密事件显示🔒标签

全部通过 = ✅ 修复完成！

---

## 📖 下一步

现在 MVP 已经是一个可用的产品了！可以：
1. 邀请朋友体验
2. 录制演示视频
3. 写技术博客
4. 开源到 GitHub

享受观看 AI 博弈的乐趣吧！🎮
